#!/usr/bin/env bash
set -euo pipefail

if ! command -v rofi >/dev/null 2>&1; then
  echo "[ERROR] rofi no esta instalado." >&2
  exit 1
fi

# Modo drun: solo aplicaciones .desktop.
exec rofi -show drun -modi drun "$@"
